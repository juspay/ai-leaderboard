services:
  app:
    build: .
    ports:
      - "${APP_PORT:-8420}:8420"
    environment:
      - PORT=8420
      - CLAUDE_DB_PATH=/data/leaderboard.db
      - CLAUDE_PLAN_COST=200
    volumes:
      - app-data:/data

volumes:
  app-data:
