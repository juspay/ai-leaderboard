services:
  app:
    build: .
    ports:
      - "${APP_PORT:-3000}:3000"
    environment:
      - PORT=3000
      - CLAUDE_DB_PATH=/data/leaderboard.db
      - CLAUDE_PLAN_COST=200
    volumes:
      - app-data:/data

volumes:
  app-data:
