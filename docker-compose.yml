services:
  app:
    build: .
    ports:
      - "${APP_PORT:-3000}:3000"
    environment:
      - PORT=3000
      - DB_PATH=/data/leaderboard.db
      - PLAN_COST=200
    volumes:
      - app-data:/data

volumes:
  app-data:
